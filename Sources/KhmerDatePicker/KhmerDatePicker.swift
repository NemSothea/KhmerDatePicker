// KhmerDatePicker — public umbrella file.
//
// All public API lives in submodule files; this file exists so the package
// has a discoverable root and to host package-level documentation.

import Foundation

public enum KhmerDatePickerInfo {
    public static let version = "1.0.0"
}
